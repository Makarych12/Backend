// Модуль 32 — Linux и терминал с нуля.
// Практика — в виртуальном Linux-терминале (поле `linuxLab`, движок src/utils/virtualShell.js):
// команды выполняются по-настоящему над файловой системой в памяти, а check() проверяет
// РЕЗУЛЬТАТ (файлы, права, процессы), а не текст команды — любой верный путь засчитывается.

// Маленькая "система" для свободного исследования в теории первого урока.
const MINI_SYSTEM = {
  home: {
    user: {
      'notes.txt': 'Купить молоко\nВыучить Linux\nДоделать API\n',
      projects: {
        'shop-api': {
          'main.py': 'from fastapi import FastAPI\n\napp = FastAPI()\n\n\n@app.get("/")\ndef root():\n    return {"status": "ok"}\n',
          'requirements.txt': 'fastapi\nuvicorn\n',
          'README.md': '# Shop API\n',
        },
      },
      downloads: { 'photo.jpg': '', 'archive.zip': '' },
      '.bashrc': '# настройки твоего терминала\nalias ll="ls -la"\n',
    },
  },
  etc: {
    type: 'dir',
    owner: 'root',
    children: {
      hostname: { type: 'file', content: 'backend-01\n', owner: 'root' },
      'os-release': { type: 'file', content: 'PRETTY_NAME="Ubuntu 24.04.1 LTS"\nNAME="Ubuntu"\n', owner: 'root' },
      shadow: { type: 'file', content: 'root:$6$rounds=65536$secret:19000:0:99999:7:::\n', owner: 'root', mode: '600' },
    },
  },
  var: {
    type: 'dir',
    owner: 'root',
    children: {
      log: {
        type: 'dir',
        owner: 'root',
        children: {
          'syslog': { type: 'file', content: 'Sep 13 10:00:01 backend-01 systemd[1]: Started Daily apt upgrade and clean activities.\nSep 13 10:00:02 backend-01 sshd[634]: Accepted publickey for user from 203.0.113.7\n', owner: 'root' },
        },
      },
    },
  },
  tmp: { type: 'dir', mode: '777' },
};

export const module32 = {
  id: 'linux-basics',
  order: 32,
  title: 'Linux и терминал с нуля',
  icon: '🐧',
  description:
    'Файлы, каталоги, права, процессы и конвейеры команд — всё, что нужно, чтобы уверенно чувствовать себя на сервере без мышки и окон.',
  lessons: [
    // ------------------------------------------------------------------
    // Урок 1. Навигация и файлы
    // ------------------------------------------------------------------
    {
      id: 'linux-navigation-files',
      title: 'Навигация и файлы: pwd, ls, cd, mkdir, cp, mv, rm',
      summary: 'Как устроена файловая система Linux и как ходить по ней, создавать, копировать, переименовывать и удалять файлы — только текстом, без мышки',
      theory: [
        {
          type: 'p',
          text: 'Почти все серверы в мире работают на Linux. И у них нет ни окон, ни рабочего стола, ни мышки — только чёрное окно с мигающим курсором. Когда ты задеплоишь свой FastAPI-сервер (вспомни модуль 13) и что-то пойдёт не так, тебе придётся подключиться к серверу и разобраться. А всё, что дома ты делал мышкой — открыть папку, создать файл, переименовать, удалить — там делается словами. Эти слова называются командами, а окно, куда их печатают, — терминалом.',
        },
        {
          type: 'analogy',
          text: 'Файловая система — это огромный шкаф с коробками. Сам шкаф называется корнем и обозначается одним символом «/». Внутри шкафа лежат коробки (каталоги, они же папки), в коробках — другие коробки или сами вещи (файлы). Путь /home/user/projects/main.py — это инструкция: «открой шкаф → возьми коробку home → в ней коробку user → в ней projects → достань вещь main.py». Терминал всегда «стоит» у какой-то одной коробки — она называется текущим каталогом. Команда pwd отвечает на вопрос «а у какой коробки я сейчас стою?».',
        },
        {
          type: 'command',
          command: 'ls -la /home/user',
          parts: [
            { text: 'ls', desc: 'команда «покажи список» — что лежит в каталоге (от английского list)' },
            { text: '-l', desc: 'флаг «длинный формат»: не только имена, но и права, владелец, размер, дата — по одной строке на файл' },
            { text: '-a', desc: 'флаг «all» — показать и скрытые файлы, чьи имена начинаются с точки (.bashrc, .env). Флаги можно склеивать: -la = -l -a' },
            { text: '/home/user', desc: 'какой каталог показать. Если не указать — покажет текущий' },
          ],
          result: 'Терминал выведет таблицу: drwxr-xr-x — это каталог (буква d в начале), -rw-r--r-- — обычный файл (дефис в начале). В первом уроке нас интересуют только имена; что значат буквы rwx — разберём в следующем уроке.',
        },
        {
          type: 'steps',
          title: 'Девять команд, которых хватит на 90% работы с файлами',
          items: [
            { code: 'pwd', note: '«Где я?» — печатает полный путь текущего каталога (print working directory)' },
            { code: 'ls', note: '«Что здесь лежит?» — список файлов и каталогов' },
            { code: 'cd projects', note: '«Зайди в коробку projects». cd .. — выйти на уровень вверх, cd без аргумента — домой, в /home/user' },
            { code: 'mkdir reports', note: '«Сделай новую коробку» с именем reports (make directory). mkdir -p a/b/c — создать сразу всю цепочку' },
            { code: 'touch todo.txt', note: '«Создай пустой файл» todo.txt (если файл уже есть — ничего не сломает)' },
            { code: 'cat todo.txt', note: '«Покажи содержимое файла» целиком прямо в терминале' },
            { code: 'cp todo.txt copy.txt', note: '«Скопируй» todo.txt в copy.txt. Для каталогов нужен флаг -r (рекурсивно, со всем содержимым)' },
            { code: 'mv old.txt new.txt', note: '«Перемести». Это же и переименование: если второй аргумент — каталог, файл переедет в него; если имя — файл получит новое имя' },
            { code: 'rm copy.txt', note: '«Удали» файл. rm -r каталог — удалить каталог со всем содержимым' },
          ],
        },
        {
          type: 'list',
          title: 'Три способа написать путь',
          items: [
            'Абсолютный — начинается с «/», от самого шкафа: /home/user/projects. Работает из любого места, длинный, но однозначный.',
            'Относительный — от текущего каталога: projects/main.py означает «в коробке projects, которая лежит здесь». Точка «.» — текущий каталог, две точки «..» — родительский (на уровень выше).',
            'Домашний — «~» (тильда) всегда означает твой домашний каталог /home/user, откуда бы ты ни писал: cd ~/projects.',
          ],
        },
        {
          type: 'callout',
          variant: 'warning',
          title: 'В Linux нет корзины',
          text: 'rm удаляет файл сразу и навсегда — никакого «восстановить из корзины». А rm -rf каталог удалит его со всем содержимым, не задавая вопросов (-f = force, «не спрашивай»). Прежде чем нажать Enter после rm, перечитай команду ещё раз: особенно проверь, что не написал rm -rf / или rm -rf ~ — это стирает всю систему или весь твой домашний каталог. В нашем учебном терминале от этих двух команд стоит защита, на настоящем сервере — нет.',
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'Как не печатать всё руками',
          text: 'В настоящем терминале клавиша Tab дописывает имя файла за тебя (напиши cd pro и нажми Tab — получишь cd projects/), а стрелка вверх возвращает предыдущие команды. Стрелки вверх/вниз работают и в терминале ниже — пользуйся, чтобы не набирать одно и то же.',
        },
        {
          type: 'p',
          text: 'Поброди по маленькой системе ниже. Она устроена как настоящая Ubuntu: твой дом — /home/user, настройки системы — в /etc, журналы — в /var/log. Попробуй зайти в каждую коробку, посмотреть, что там, и вернуться домой. Сломать здесь ничего нельзя — кнопка «Сбросить» вернёт всё как было.',
        },
        {
          type: 'linuxTerminal',
          title: 'Полигон: походи по системе',
          initialFs: MINI_SYSTEM,
          welcome: 'Ты в своём домашнем каталоге /home/user. Начни с pwd и ls.',
          suggestions: ['pwd', 'ls', 'ls -la', 'cd projects', 'cd ..', 'cat notes.txt', 'ls /etc', 'cat /etc/hostname', 'cd ~'],
        },
      ],
      example: {
        title: 'Типичная сессия на сервере: разложить файлы по местам',
        lang: 'bash',
        code: `user@backend:~$ pwd
/home/user

user@backend:~$ ls
downloads  notes.txt  projects

user@backend:~$ mkdir -p projects/blog/static      # сразу вся цепочка каталогов
user@backend:~$ cd projects/blog
user@backend:~/projects/blog$ touch app.py         # пустой файл-заготовка
user@backend:~/projects/blog$ ls
app.py  static

user@backend:~/projects/blog$ mv app.py main.py    # переименовали
user@backend:~/projects/blog$ cp main.py static/   # скопировали в каталог
user@backend:~/projects/blog$ ls static
main.py

user@backend:~/projects/blog$ rm static/main.py    # передумали — удалили копию
user@backend:~/projects/blog$ cd                   # домой одним словом
user@backend:~$`,
        explanation: 'Обрати внимание на приглашение слева от команды: user@backend:~/projects/blog$ — терминал всегда подсказывает, где ты находишься (~ = /home/user). Если потерялся — pwd покажет полный путь, cd без аргумента вернёт домой.',
      },
      linuxLab: {
        initialFs: {
          home: {
            user: {
              'draft.txt': 'Отчёт за сентябрь: сервер работал стабильно, 0 инцидентов.\n',
              documents: { 'readme.txt': 'Здесь лежат готовые отчёты.\n' },
              downloads: { 'photo.jpg': '' },
            },
          },
        },
        goal: 'Файл draft.txt лежит не там и называется не так. Переименуй его в report.txt и положи в каталог documents. В итоге должен появиться documents/report.txt с тем же текстом, а draft.txt из домашнего каталога — исчезнуть.',
        check: (shell) => {
          const original = 'Отчёт за сентябрь: сервер работал стабильно, 0 инцидентов.\n';
          return shell.readFile('/home/user/documents/report.txt') === original && !shell.exists('/home/user/draft.txt');
        },
        hint: 'Переименование и перемещение в Linux — это одна и та же команда: у неё два аргумента, «что» и «куда», а «куда» может быть и новым именем, и путём внутри другого каталога. Можно сделать в два шага, а можно в один.',
        solution: `# Одной командой: переместить И переименовать сразу
mv draft.txt documents/report.txt

# Или в два шага
mv draft.txt report.txt
mv report.txt documents/

# Проверить результат
ls documents
cat documents/report.txt`,
        suggestions: ['ls', 'cat draft.txt', 'ls documents'],
      },
      tasks: [
        {
          title: 'Задание 1: вложенные каталоги одной командой',
          difficulty: 'easy',
          lang: 'bash',
          description: 'В терминале выше создай структуру projects/shop/app (три вложенных каталога) ОДНОЙ командой, а затем убедись, что она появилась — покажи всё дерево каталога projects.',
          hints: [
            'У mkdir есть флаг, который создаёт всю цепочку родителей, если их ещё нет.',
            'Показать всё дерево можно командой find projects (она выводит все пути внутри) или tree projects.',
          ],
          solution: `mkdir -p projects/shop/app
find projects
# projects
# projects/shop
# projects/shop/app`,
        },
        {
          title: 'Задание 2: резервная копия и уборка',
          difficulty: 'medium',
          lang: 'bash',
          description: 'Сделай копию documents/readme.txt под именем readme_backup.txt в каталоге downloads. Проверь через cat, что копия содержит тот же текст. Потом удали каталог downloads целиком, со всем содержимым, и убедись через ls, что его больше нет.',
          hints: [
            'У cp два аргумента: откуда и куда. «Куда» можно указать вместе с новым именем: каталог/новое_имя.',
            'rm откажется удалять каталог просто так — прочитай, что он ответит, и вспомни флаг для каталогов.',
          ],
          solution: `cp documents/readme.txt downloads/readme_backup.txt
cat downloads/readme_backup.txt
rm -r downloads
ls`,
        },
        {
          title: 'Задание 3: работа по абсолютным путям',
          difficulty: 'hard',
          lang: 'bash',
          description: 'В полигоне из теории (терминал «Полигон: походи по системе»): не покидая домашний каталог, посмотри список файлов в /etc и выведи содержимое /etc/hostname. Потом попробуй прочитать /etc/shadow — и объясни себе, почему терминал отказал. В конце одной командой перейди в /var/log, посмотри, что там, и вернись домой ОДНИМ словом.',
          hints: [
            'Абсолютный путь начинается с / и работает из любого места — cd не нужен.',
            '/etc/shadow хранит хеши паролей всех пользователей, поэтому читать его может только администратор (root). Подробно про права — в следующем уроке.',
            'cd без аргумента — это «домой».',
          ],
          solution: `ls /etc
cat /etc/hostname
cat /etc/shadow        # cat: /etc/shadow: Permission denied — файл только для root
cd /var/log && ls
cd`,
        },
      ],
      mistakes: [
        {
          wrong: 'Написать cd projects/ — получить ошибку No such file or directory — и решить, что «терминал сломался»',
          right: 'Ошибка означает ровно то, что написано: в ТЕКУЩЕМ каталоге нет коробки projects. Сначала pwd («где я?») и ls («что здесь есть?»), а потом cd — относительный путь всегда считается от того места, где ты стоишь',
        },
        {
          wrong: 'Считать, что mv — только для перемещения, и искать отдельную команду rename для переименования',
          right: 'mv old.txt new.txt — это и есть переименование. Для Linux «переименовать» и «переместить в тот же каталог под другим именем» — одно и то же действие',
        },
      ],
      checklist: [
        'Понимаю, что файловая система — дерево каталогов от корня «/», а терминал всегда стоит в каком-то текущем каталоге',
        'Различаю абсолютный путь (/home/user/x), относительный (projects/x) и домашний (~/x); знаю, что значат . и ..',
        'Умею ориентироваться: pwd, ls, ls -la, cd, cd .., cd без аргумента',
        'Умею создавать и менять файлы: mkdir -p, touch, cat, cp, mv (включая переименование), rm и rm -r',
        'Помню, что rm удаляет безвозвратно, и перечитываю команду перед Enter',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 2. Права доступа
    // ------------------------------------------------------------------
    {
      id: 'linux-permissions',
      title: 'Права доступа: r, w, x и chmod',
      summary: 'Почему сервер отвечает Permission denied, что значат буквы rwx и цифры 755, как сделать скрипт исполняемым и когда нужен sudo',
      theory: [
        {
          type: 'p',
          text: 'На одном сервере одновременно живут много пользователей и программ: ты, твой сосед-разработчик, база данных, веб-сервер nginx. Если бы каждый мог читать и менять любые файлы, один неаккуратный rm или чужой взломанный сервис уничтожили бы всё. Поэтому у каждого файла и каталога в Linux есть права: кто может его читать, кто — менять, кто — запускать. Ошибка Permission denied, которую ты видел в прошлом уроке, — это не поломка, а именно эта защита в действии.',
        },
        {
          type: 'analogy',
          text: 'У каждого файла — замок с тремя разными ключами. Первый ключ — у владельца файла (обычно того, кто его создал). Второй — у группы (например, у всех разработчиков команды). Третий — у всех остальных. И каждый ключ открывает не всё подряд, а ровно то, что на нём написано: r (read) — можно посмотреть, w (write) — можно изменить, x (execute) — можно запустить как программу. Владелец может иметь ключ «читать и менять», а все остальные — только «читать». Так секретный файл с паролями остаётся секретным, даже если на сервере сто пользователей.',
        },
        {
          type: 'command',
          command: 'ls -l deploy.sh',
          parts: [
            { text: '-', desc: 'первый символ — тип: дефис = обычный файл, d = каталог' },
            { text: 'rwx', desc: 'права ВЛАДЕЛЬЦА: читать, писать, запускать — все три ключа' },
            { text: 'r-x', desc: 'права ГРУППЫ: читать и запускать, но не менять (дефис на месте w = права нет)' },
            { text: 'r--', desc: 'права ВСЕХ ОСТАЛЬНЫХ: только читать' },
            { text: 'user user', desc: 'два имени подряд: владелец файла и группа файла' },
          ],
          result: 'Полный вывод выглядит так: -rwxr-xr-- 1 user user 58 Sep 13 10:00 deploy.sh. Девять букв после типа всегда читаются тройками: владелец / группа / остальные.',
        },
        {
          type: 'steps',
          title: 'Откуда берутся цифры 755 и 644',
          items: [
            { code: 'r = 4,  w = 2,  x = 1', note: 'У каждого права — своё число. Права одной тройки складываются в одну цифру' },
            { code: 'rwx = 4+2+1 = 7', note: 'Всё можно' },
            { code: 'r-x = 4+0+1 = 5', note: 'Читать и запускать, менять нельзя' },
            { code: 'rw- = 4+2+0 = 6', note: 'Читать и менять, запускать нельзя (обычный текстовый файл)' },
            { code: 'r-- = 4', note: 'Только читать' },
            { code: 'chmod 755 deploy.sh', note: 'Три цифры = три тройки: владелец 7 (rwx), группа 5 (r-x), остальные 5 (r-x). Стандарт для скриптов и программ' },
            { code: 'chmod 644 notes.txt', note: 'Владелец rw-, все остальные r--. Стандарт для обычных файлов' },
            { code: 'chmod 600 secret.key', note: 'Только владелец, и только читать/менять. Так хранят ключи и пароли' },
          ],
        },
        {
          type: 'list',
          title: 'Второй способ: буквами (удобно, когда нужно поменять одно право)',
          items: [
            'chmod +x deploy.sh — добавить право запуска всем (самая частая команда в жизни разработчика: «сделай скрипт исполняемым»).',
            'chmod u+x deploy.sh — то же, но только владельцу (u = user-владелец, g = group, o = others, a = all).',
            'chmod g-w report.txt — забрать у группы право писать. Плюс добавляет право, минус забирает, знак равно — устанавливает ровно такие.',
            'chmod -R 755 projects — применить рекурсивно ко всему каталогу и всему, что внутри.',
          ],
        },
        {
          type: 'p',
          text: 'Для каталогов те же буквы значат чуть другое: r — можно посмотреть список файлов внутри (ls), w — можно создавать и удалять файлы внутри, x — можно ВОЙТИ в каталог (cd) и добраться до файлов. Поэтому у каталогов почти всегда стоит x: каталог с правами 644 бесполезен — в него нельзя даже зайти.',
        },
        {
          type: 'callout',
          variant: 'warning',
          title: 'chmod 777 — «а давайте просто всем всё разрешим»',
          text: 'Самый частый совет из старых форумов и самая опасная привычка. 777 означает, что ЛЮБОЙ пользователь и ЛЮБАЯ программа на сервере может переписать твой файл. Если взломают хотя бы один сервис — он перепишет твой скрипт деплоя, и ты сам его запустишь. Получил Permission denied — разберись, кто владелец и какие права нужны, а не раздавай всё всем. Для скриптов достаточно 755, для данных — 644, для секретов — 600.',
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'sudo: «попросить администратора»',
          text: 'Есть особый пользователь root — администратор, для которого замков не существует. Файлы системы (/etc, /var/log) принадлежат ему, поэтому обычный пользователь получает Permission denied. sudo команда («superuser do») выполняет одну команду от имени root — так ставят программы и правят настройки. Менять права (chmod) и владельца (chown) чужого файла тоже может только root: попытка без sudo даст Operation not permitted. Правило: sudo — только когда точно понимаешь, зачем, и никогда не «на всякий случай».',
        },
      ],
      example: {
        title: 'Классика: «я написал скрипт, а он не запускается»',
        lang: 'bash',
        code: `user@backend:~$ cat deploy.sh
#!/bin/bash
echo "Deploy started"

user@backend:~$ ./deploy.sh
bash: ./deploy.sh: Permission denied      # ключа x нет — запуск запрещён

user@backend:~$ ls -l deploy.sh
-rw-r--r-- 1 user user 34 Sep 13 10:00 deploy.sh   # rw-: читать, писать. x нет

user@backend:~$ chmod +x deploy.sh        # выдаём право запуска
user@backend:~$ ls -l deploy.sh
-rwxr-xr-x 1 user user 34 Sep 13 10:00 deploy.sh   # появились x

user@backend:~$ ./deploy.sh
Deploy started

user@backend:~$ cat /etc/shadow
cat: /etc/shadow: Permission denied      # файл root, права 600
user@backend:~$ sudo cat /etc/shadow
root:$6$rounds=65536$secret:19000:0:99999:7:::`,
        explanation: 'Скрипт запускают через ./deploy.sh — точка со слэшем значит «из текущего каталога»; без неё терминал искал бы программу deploy.sh среди системных команд и не нашёл. Первая строка #!/bin/bash говорит системе, какой программой выполнять файл. Подробнее о скриптах — в следующем модуле.',
      },
      linuxLab: {
        initialFs: {
          home: {
            user: {
              'deploy.sh': {
                type: 'file',
                mode: '644',
                content: '#!/bin/bash\necho "Deploy started"\necho "Copying files to /var/www..."\necho "deploy ok" > deploy.log\necho "Done"\n',
              },
              'notes.txt': 'Не забыть: перед деплоем прогнать тесты.\n',
              'secret.key': { type: 'file', content: 'sk-live-9f8e7d6c5b4a\n', owner: 'root', mode: '600' },
            },
          },
        },
        goal: 'Скрипт deploy.sh отказывается запускаться. Выдай ему право на выполнение и запусти — в терминале должно появиться «Deploy started». Права «всем всё» (777) — не решение: хватит стандартных для скрипта.',
        check: (shell) => {
          const script = shell.getNode('/home/user/deploy.sh');
          if (!script || script.type !== 'file') return false;
          const ownerCanExecute = (Number(script.mode[0]) & 1) === 1;
          const notWorldWritable = (Number(script.mode[2]) & 2) === 0;
          const ran = (shell.readFile('/home/user/deploy.log') || '').includes('deploy ok');
          return ownerCanExecute && notWorldWritable && ran;
        },
        hint: 'Сначала посмотри на права файла длинным списком и найди, какого ключа не хватает в первой тройке. Добавить его можно и буквами, и цифрами — оба способа из теории подходят. Запускается скрипт с указанием «из текущего каталога».',
        solution: `ls -l deploy.sh          # -rw-r--r-- — нет x
chmod +x deploy.sh       # или chmod 755 deploy.sh, или chmod u+x deploy.sh
./deploy.sh`,
        suggestions: ['ls -l', './deploy.sh', 'cat deploy.sh'],
      },
      tasks: [
        {
          title: 'Задание 1: чужой секрет',
          difficulty: 'easy',
          lang: 'bash',
          description: 'В терминале выше посмотри права и владельца файла secret.key. Попробуй прочитать его обычным cat, прочитай ошибку и объясни себе по буквам прав, почему тебе отказали. Затем прочитай файл так, как это сделал бы администратор.',
          hints: [
            'ls -l покажет, что владелец — root, а права -rw------- : у «всех остальных» (третья тройка) нет даже r.',
            'Администратор — root, а выполнить одну команду от его имени можно через sudo.',
          ],
          solution: `ls -l secret.key      # -rw------- 1 root root ... — только root может читать
cat secret.key        # cat: secret.key: Permission denied
sudo cat secret.key`,
        },
        {
          title: 'Задание 2: спрятать заметки',
          difficulty: 'medium',
          lang: 'bash',
          description: 'Сделай так, чтобы notes.txt мог читать и менять только владелец, а группа и все остальные не могли даже читать. Проверь результат через ls -l — первая тройка должна остаться rw-, две другие превратиться в ---.',
          hints: [
            'Считай цифры: владелец rw- = 4+2, остальные ничего = 0.',
            'Буквами это можно записать как «забрать у группы и остальных право читать»: g и o через запятую или вместе — go-r.',
          ],
          solution: `chmod 600 notes.txt      # или chmod go-r notes.txt
ls -l notes.txt          # -rw------- 1 user user ...`,
        },
        {
          title: 'Задание 3: запертый каталог',
          difficulty: 'hard',
          lang: 'bash',
          description: 'Создай каталог private и положи в него файл plan.txt. Выставь каталогу права 700 и убедись, что ты по-прежнему можешь зайти внутрь. Потом выставь 000 и попробуй cd private и ls private — объясни, почему даже владельцу отказано и какой буквы не хватает для входа, а какой для просмотра списка. В конце верни каталогу 755.',
          hints: [
            '700 = владельцу rwx, остальным ничего. Владелец всё ещё имеет x, поэтому cd работает.',
            'При 000 у владельца нет x (нельзя войти) и нет r (нельзя посмотреть список). Права проверяются даже для владельца — root единственный, кому замки не мешают.',
          ],
          solution: `mkdir private && touch private/plan.txt
chmod 700 private
cd private && cd ..          # работает: у владельца есть x
chmod 000 private
cd private                   # bash: cd: private: Permission denied — нет x
ls private                   # ls: cannot open directory 'private': Permission denied — нет r
chmod 755 private`,
        },
      ],
      mistakes: [
        {
          wrong: 'Увидеть Permission denied и сразу написать sudo chmod 777 — «чтобы точно заработало»',
          right: 'Сначала ls -l: кто владелец и какие права. Обычно не хватает одного x (chmod +x) или файл принадлежит root и нужен sudo для одной команды. 777 открывает файл всем программам на сервере — это дыра, а не решение',
        },
        {
          wrong: 'Запускать скрипт как deploy.sh и получать command not found',
          right: 'Терминал ищет команды в системных каталогах, а не в текущем. Скрипт из текущего каталога запускают как ./deploy.sh — точка и слэш значат «вот здесь»',
        },
      ],
      checklist: [
        'Читаю строку ls -l по тройкам: владелец / группа / остальные, и знаю, что значат r, w, x для файла и для каталога',
        'Умею переводить права в цифры (r=4, w=2, x=1) и знаю стандарты: 755 для скриптов, 644 для файлов, 600 для секретов',
        'Умею chmod +x, chmod u+x, chmod 755 — и запускаю скрипт через ./имя',
        'Понимаю, что root — администратор без замков, а sudo выполняет одну команду от его имени',
        'Не использую chmod 777 и sudo «на всякий случай»',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 3. Процессы
    // ------------------------------------------------------------------
    {
      id: 'linux-processes',
      title: 'Процессы: ps, top, kill',
      summary: 'Что такое процесс и PID, как увидеть, что сейчас работает на сервере, кто ест память, и как правильно остановить зависшую программу',
      theory: [
        {
          type: 'p',
          text: 'Программа на диске — это просто файл. Как только ты её запустил, она становится процессом: живым, работающим, занимающим память и процессор. Твой FastAPI-сервер на настоящем сервере — это процесс uvicorn. База данных — процесс postgres. Даже сам терминал, в котором ты печатаешь, — процесс bash. Когда «сайт завис» или «сервер тормозит», первым делом смотрят на процессы: кто работает, кто сколько ест и кого пора остановить.',
        },
        {
          type: 'analogy',
          text: 'Сервер — это цех, а процессы — рабочие на смене. У каждого рабочего на груди бейдж с номером — это PID (process ID), уникальный номер процесса. Команда ps — список «кто сейчас на смене». top — табло в реальном времени: кто больше всех нагружает станок (CPU) и кто занял больше всех места на складе (память). А kill — способ отправить рабочему сообщение. Обычный kill — вежливое «смена окончена, убери за собой и уходи». kill -9 — охрана выводит силой, не давая ни доделать, ни убрать за собой.',
        },
        {
          type: 'command',
          command: 'ps aux',
          parts: [
            { text: 'ps', desc: 'команда «покажи процессы» (process status)' },
            { text: 'aux', desc: 'три склеенных флага: a — процессы всех пользователей, u — в удобном формате с владельцем и нагрузкой, x — включая те, что запущены не из терминала (службы). Пишутся без дефиса — историческая особенность' },
          ],
          result: 'Таблица со столбцами: USER (кто запустил), PID (номер), %CPU и %MEM (сколько процессора и памяти ест), COMMAND (что именно запущено). Обычно строк много, поэтому ps aux почти всегда сочетают с поиском по имени — ps aux | grep uvicorn (про | — в следующем уроке, пока просто пользуйся как «найди в выводе слово»).',
        },
        {
          type: 'steps',
          title: 'Стандартный порядок действий, когда «что-то зависло»',
          items: [
            { code: 'ps aux', note: 'Посмотреть всех. Или top — то же, но отсортировано по нагрузке и обновляется само (выход — клавиша q)' },
            { code: 'ps aux | grep python', note: 'Найти нужный процесс по имени и узнать его PID' },
            { code: 'kill 3117', note: 'Вежливо попросить процесс с PID 3117 завершиться. Большинство программ послушно закрывают файлы и соединения и выходят' },
            { code: 'ps aux | grep python', note: 'Проверить, что процесс исчез. Если через несколько секунд всё ещё висит — он не отвечает' },
            { code: 'kill -9 3117', note: 'Крайняя мера: система убивает процесс мгновенно, не спрашивая. Только если обычный kill не помог' },
            { code: 'pkill python', note: 'То же самое, но по имени, без поиска PID. Осторожно: остановит ВСЕ процессы с таким именем' },
          ],
        },
        {
          type: 'list',
          title: 'Сигналы и горячие клавиши, о которых спрашивают на собеседовании',
          items: [
            'Ctrl+C — остановить программу, которая работает прямо сейчас в твоём терминале (например, uvicorn, запущенный руками). Это тот же вежливый сигнал, что и kill.',
            'kill PID — посылает сигнал TERM (15): «завершись по-хорошему». kill -9 PID — сигнал KILL (9): убить немедленно, программа даже не узнает.',
            'Ctrl+Z — поставить программу на паузу и вернуть себе терминал; bg — продолжить её в фоне, fg — вернуть на передний план. Команда с & в конце (python bot.py &) сразу запускается в фоне.',
            'В нашем учебном терминале нет настоящих фоновых задач, поэтому fg и bg честно отвечают «no such job» — но на сервере ты будешь пользоваться ими постоянно.',
          ],
        },
        {
          type: 'callout',
          variant: 'warning',
          title: 'Почему не начинать сразу с kill -9',
          text: 'После вежливого kill база данных успевает дописать данные на диск, сервер — закрыть соединения с клиентами, программа — удалить временные файлы. kill -9 обрывает всё на полуслове: можно получить повреждённые данные или «мусор», который придётся чистить руками. И никогда не трогай PID 1 — это init/systemd, родитель всех процессов; система его защищает, но на некоторых машинах его остановка равна выключению сервера.',
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'Чужие процессы',
          text: 'Остановить можно только свой процесс. Попытка kill на процессе другого пользователя (например, postgres) даст Operation not permitted — ровно как с правами на файлы из прошлого урока. Администратор (sudo kill) может остановить любой — поэтому и здесь sudo только тогда, когда точно понимаешь, что делаешь.',
        },
      ],
      example: {
        title: 'Находим и останавливаем зависший процесс',
        lang: 'bash',
        code: `user@backend:~$ ps aux
USER         PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
root           1  0.0  0.1  ...                            /sbin/init
postgres     613  0.4  3.1  ...                            postgres -D /var/lib/postgresql/data
user        1204  1.5  4.2  ...                            uvicorn main:app --host 0.0.0.0 --port 8000
user        3117  2.3 71.4  ...                            python report_generator.py

user@backend:~$ kill 3117            # вежливо: сигнал TERM
user@backend:~$ ps aux | grep python
user@backend:~$                      # пусто — процесс завершился

user@backend:~$ kill 613
bash: kill: (613) - Operation not permitted   # чужой процесс (postgres)

user@backend:~$ kill 99999
bash: kill: (99999) - No such process         # такого PID нет`,
        explanation: 'Столбец %MEM сразу выдал виновника: report_generator.py занял 71% памяти. Обычного kill хватило — процесс исчез из списка. Обрати внимание: kill отвечает молчанием, когда всё прошло хорошо; в Linux тишина — это «ок».',
      },
      linuxLab: {
        initialFs: {
          home: {
            user: {
              'report_generator.py': 'import time\n\nrows = []\nwhile True:\n    rows.append("x" * 10_000_000)  # утечка памяти: список растёт бесконечно\n    time.sleep(1)\n',
              'main.py': 'from fastapi import FastAPI\n\napp = FastAPI()\n',
            },
          },
        },
        processes: [
          { pid: 1, user: 'root', cpu: 0.0, mem: 0.1, command: '/sbin/init' },
          { pid: 412, user: 'root', cpu: 0.0, mem: 0.2, command: 'sshd: /usr/sbin/sshd -D' },
          { pid: 613, user: 'postgres', cpu: 0.4, mem: 3.1, command: 'postgres -D /var/lib/postgresql/data' },
          { pid: 977, user: 'user', cpu: 0.0, mem: 0.3, command: '-bash' },
          { pid: 1204, user: 'user', cpu: 1.5, mem: 4.2, command: 'uvicorn main:app --host 0.0.0.0 --port 8000' },
          { pid: 2381, user: 'user', cpu: 12.0, mem: 6.8, command: 'node build.js --watch' },
          { pid: 3117, user: 'user', cpu: 2.3, mem: 71.4, command: 'python report_generator.py' },
        ],
        goal: 'Сервер еле дышит: один из процессов съел почти всю память. Найди его и останови. Сервер uvicorn и база данных postgres при этом должны продолжать работать — «убить всё подряд» не считается.',
        check: (shell) =>
          shell.getProcess(3117) === null && shell.getProcess(1204) !== null && shell.getProcess(613) !== null,
        hint: 'Сначала посмотри на всех: тебе нужен столбец с процентом памяти. Запомни номер из столбца PID у самого прожорливого — и отправь этому номеру сигнал завершения. Проверь, что он пропал из списка.',
        solution: `ps aux                  # или top — ищем самый большой %MEM
kill 3117               # вежливо
ps aux | grep python    # пусто — готово

# Альтернативы:
# kill -9 3117          — если бы обычный kill не подействовал
# pkill python          — по имени, без PID`,
        suggestions: ['ps aux', 'top', 'ps aux | grep python'],
      },
      tasks: [
        {
          title: 'Задание 1: найди сервер',
          difficulty: 'easy',
          lang: 'bash',
          description: 'В терминале выше выведи список всех процессов и найди PID и владельца процесса, который обслуживает твоё FastAPI-приложение (подсказка: он называется не «fastapi»). Сколько всего процессов принадлежит пользователю user?',
          hints: [
            'FastAPI сам по себе не запускается — его «включает» uvicorn (вспомни модуль 5).',
            'Просто ps без флагов показывает только процессы текущего пользователя — удобно, чтобы посчитать свои.',
          ],
          solution: `ps aux | grep uvicorn     # user 1204 ... uvicorn main:app
ps                        # только свои: bash, uvicorn, node, python — четыре штуки`,
        },
        {
          title: 'Задание 2: остановить по имени',
          difficulty: 'medium',
          lang: 'bash',
          description: 'Сборщик фронтенда node build.js --watch крутится впустую и грузит процессор. Останови его, не выясняя PID вручную — по имени программы. Убедись, что uvicorn остался жив.',
          hints: [
            'Есть команда, которая делает «найди процесс по имени и пошли ему сигнал» одним действием.',
            'Имя программы — это первое слово в столбце COMMAND.',
          ],
          solution: `pkill node
ps aux                    # node исчез, uvicorn 1204 на месте`,
        },
        {
          title: 'Задание 3: чужие процессы и катастрофа на сервере',
          difficulty: 'hard',
          lang: 'bash',
          description: 'Попробуй остановить postgres (PID 613) обычным kill и прочитай ответ. Потом попробуй sudo kill 1 и тоже прочитай ответ. Наконец, останови через sudo процесс sshd (PID 412) — получится. Объясни себе: почему на НАСТОЯЩЕМ сервере эта последняя команда была бы катастрофой, если ты подключён к нему через интернет?',
          hints: [
            'postgres принадлежит пользователю postgres — как с файлами, чужое трогать нельзя без sudo.',
            'PID 1 защищён всегда, даже от root.',
            'sshd — это служба, через которую ты подключаешься к серверу по сети (ssh). Убил её — и следующая команда уже никуда не долетит: ты отрезал сук, на котором сидишь. Придётся идти к хостеру за консолью.',
          ],
          solution: `kill 613              # bash: kill: (613) - Operation not permitted
sudo kill 1           # bash: kill: (1) - Operation not permitted
sudo kill 412         # выполнилось молча... и на реальном сервере ты только что потерял к нему доступ
ps aux                # sshd больше нет`,
        },
      ],
      mistakes: [
        {
          wrong: 'Сразу писать kill -9 на всё, что мешает, — «так надёжнее»',
          right: 'Сначала обычный kill: процесс успеет закрыть файлы и соединения. -9 — только если через несколько секунд процесс всё ещё висит. Для базы данных kill -9 может обернуться повреждёнными данными',
        },
        {
          wrong: 'Запустить uvicorn в терминале, закрыть окно терминала и удивляться, что сайт «упал»',
          right: 'Процесс, запущенный из терминала, умирает вместе с ним. На сервере программы запускают как службы (systemd — следующий модуль) или хотя бы в фоне через &, а не в открытом окне',
        },
      ],
      checklist: [
        'Понимаю разницу между программой (файл на диске) и процессом (запущенная программа с PID)',
        'Умею смотреть процессы: ps aux, ps aux | grep имя, top, и читаю столбцы PID, %CPU, %MEM, COMMAND',
        'Умею останавливать: kill PID (вежливо, TERM), kill -9 PID (силой, KILL), pkill имя, Ctrl+C',
        'Знаю, что чужой процесс не остановить без sudo, а PID 1 — не трогать вообще',
        'Знаю, что Ctrl+Z / bg / fg / & управляют фоновыми задачами в терминале',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 4. Текст, поиск и конвейер
    // ------------------------------------------------------------------
    {
      id: 'linux-text-pipes',
      title: 'Текст, поиск и конвейер: grep, find, sort, | и >',
      summary: 'Самая важная тема модуля: как соединять маленькие команды в цепочку через |, искать строки в файлах и файлы на диске, и сохранять результат в файл',
      theory: [
        {
          type: 'p',
          text: 'На сервере всё — текст: логи, конфиги, вывод любой команды. Лог за день — это десятки тысяч строк, и никто не читает их глазами. Вместо этого в Linux есть набор маленьких программ, каждая из которых умеет одну вещь с текстом: grep — отобрать строки, sort — отсортировать, uniq — убрать повторы, wc — посчитать, head/tail — показать начало/конец. Сила не в них по отдельности, а в том, что их можно соединять в цепочку. Именно это умение отличает человека, который «немного знает Linux», от того, кто на сервере как дома.',
        },
        {
          type: 'analogy',
          text: 'Представь конвейерную ленту на заводе. Первый станок выдаёт заготовки — все строки лога. Лента везёт их ко второму станку, который отбирает только бракованные — строки со словом ERROR. Третий станок раскладывает их по порядку, четвёртый — считает, сколько одинаковых. Символ | (вертикальная черта, «пайп») — это и есть лента между станками: всё, что выходит из команды слева, становится входом команды справа. Ни один станок не знает о существовании соседей — каждый просто берёт, что приехало, делает своё дело и кладёт на ленту дальше.',
        },
        {
          type: 'command',
          command: 'cat app.log | grep ERROR | sort | uniq -c',
          parts: [
            { text: 'cat app.log', desc: 'станок 1: выдаёт на ленту все строки файла' },
            { text: '|', desc: 'лента: вывод слева → вход справа' },
            { text: 'grep ERROR', desc: 'станок 2: пропускает дальше только строки, где есть слово ERROR' },
            { text: '| sort', desc: 'станок 3: сортирует строки по алфавиту (одинаковые оказываются рядом)' },
            { text: '| uniq -c', desc: 'станок 4: схлопывает соседние одинаковые строки и пишет слева, сколько их было (-c = count)' },
          ],
          result: 'На выходе — сводка вида «12 ERROR Database connection refused / 3 ERROR Timeout». Четыре простые команды, одна строка — и ты знаешь, какая ошибка мучает сервер чаще всего. Так на настоящих серверах ищут причины падений.',
        },
        {
          type: 'steps',
          title: 'Станки, которые нужно знать',
          items: [
            { code: 'grep "слово" файл', note: 'Показать строки, где есть слово. -i — без учёта регистра, -n — с номерами строк, -v — наоборот, строки БЕЗ слова, -c — только посчитать' },
            { code: 'grep -r "слово" каталог', note: 'Искать во всех файлах каталога, рекурсивно. Перед каждой найденной строкой пишет имя файла' },
            { code: 'find . -name "*.log"', note: 'Найти ФАЙЛЫ по имени (а grep ищет СТРОКИ по содержимому). -type f — только файлы, -type d — только каталоги' },
            { code: 'sort', note: 'Отсортировать строки. -r — в обратном порядке, -n — как числа (чтобы 10 шло после 9, а не после 1)' },
            { code: 'uniq -c', note: 'Убрать повторы и посчитать их. Работает только с СОСЕДНИМИ строками — поэтому перед ним почти всегда sort' },
            { code: 'wc -l', note: 'Посчитать строки (word count, -l = lines). cat app.log | wc -l — сколько строк в логе' },
            { code: 'head -n 5 / tail -n 5', note: 'Первые / последние 5 строк. tail -f app.log на сервере показывает лог «вживую», по мере записи' },
            { code: 'cut -d " " -f 2', note: 'Вырезать из каждой строки колонку номер 2, считая разделителем пробел' },
          ],
        },
        {
          type: 'list',
          title: 'Перенаправление: положить результат в файл вместо экрана',
          items: [
            'команда > файл — записать вывод в файл. Если файл был — он ПЕРЕЗАПИШЕТСЯ без предупреждения.',
            'команда >> файл — дописать в конец файла, ничего не стирая. Так ведут журналы.',
            'команда 2> /dev/null — отправить сообщения об ошибках «в никуда» (/dev/null — чёрная дыра), чтобы не мешали.',
            'echo "текст" > файл — самый быстрый способ создать файл с содержимым, не открывая редактор.',
          ],
        },
        {
          type: 'callout',
          variant: 'warning',
          title: '> стирает молча',
          text: 'grep ERROR app.log > app.log — классическая ошибка: терминал сначала очищает app.log под запись, и только потом grep начинает его читать — уже пустым. Лог потерян. Правило: результат всегда пишут в ДРУГОЙ файл. И перед > с важным файлом дважды проверь имя.',
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'grep или find?',
          text: 'Путают постоянно. find ищет файлы — по имени, типу, правам: «где лежат все .log?». grep ищет строки внутри файлов — по содержимому: «в каких строках есть ERROR?». А вместе они ищут «строки с ERROR во всех .log-файлах»: find . -name "*.log" | xargs grep ERROR — xargs берёт список файлов с ленты и подставляет его как аргументы для grep. Впрочем, grep -r ERROR каталог делает то же самое короче — оба способа правильные.',
        },
      ],
      example: {
        title: 'Разбираем логи сервера, не читая их глазами',
        lang: 'bash',
        code: `user@backend:~/logs$ wc -l app.log            # сколько всего строк?
    9 app.log

user@backend:~/logs$ grep ERROR app.log        # что за ошибка?
10:00:14 ERROR Database connection refused

user@backend:~/logs$ grep -c WARN *.log        # сколько предупреждений в каждом файле?
app.log:1
db.log:1
web.log:2

user@backend:~/logs$ cat *.log | cut -d " " -f 2 | sort | uniq -c    # сводка по уровням
      1 ERROR
     12 INFO
      4 WARN

user@backend:~/logs$ grep -r ERROR . > ~/errors.txt      # сохранить ошибки в файл дома
user@backend:~/logs$ cat ~/errors.txt
./app.log:10:00:14 ERROR Database connection refused`,
        explanation: 'Звёздочка *.log — шаблон: терминал сам подставляет все файлы с таким окончанием. В сводке по уровням работают четыре станка подряд: cat склеивает все логи, cut вырезает вторую колонку (уровень), sort ставит одинаковые рядом, uniq -c считает. Такую строку пишут за десять секунд — и она заменяет полчаса прокрутки лога.',
      },
      linuxLab: {
        initialFs: {
          home: {
            user: {
              logs: {
                'app.log':
                  '10:00:01 INFO Server started on port 8000\n10:00:02 INFO Connected to database\n10:00:05 INFO GET /users 200\n10:00:07 INFO GET /users/42 200\n10:00:09 WARN Slow query: 1.8s\n10:00:11 INFO POST /orders 201\n10:00:14 ERROR Database connection refused\n10:00:15 INFO Reconnecting...\n10:00:16 INFO GET /health 200\n',
                'web.log':
                  '10:00:03 INFO GET / 200\n10:00:04 INFO GET /static/app.js 200\n10:00:08 WARN GET /admin 403\n10:00:10 INFO GET /login 200\n10:00:12 WARN GET /old-page 404\n',
                'db.log':
                  '10:00:00 INFO PostgreSQL ready to accept connections\n10:00:06 INFO checkpoint complete\n10:00:13 WARN connection limit almost reached (95/100)\n',
              },
            },
          },
        },
        goal: 'В каталоге logs три файла журналов. Где-то в них — ровно одна строка с ошибкой (ERROR). Найди её и сохрани в файл errors.txt в домашнем каталоге одной командой через конвейер — так, чтобы в errors.txt попала только строка с ошибкой, без INFO и WARN.',
        check: (shell) => {
          const text = shell.readFile('/home/user/errors.txt');
          if (!text) return false;
          return text.includes('ERROR Database connection refused') && !text.includes('INFO') && !text.includes('WARN');
        },
        hint: 'Нужны два станка и лента между ними: первый выдаёт строки всех логов (или сразу ищет по каталогу), второй отбирает строки с нужным словом. А результат вместо экрана отправляется в файл стрелкой. Не обязательно заранее знать, в каком файле ошибка.',
        solution: `# Любой из вариантов:
grep -r ERROR logs > errors.txt
cat logs/*.log | grep ERROR > errors.txt
find logs -name "*.log" | xargs grep ERROR > errors.txt

# Проверка
cat errors.txt`,
        suggestions: ['ls logs', 'cat logs/app.log', 'grep -r ERROR logs'],
      },
      tasks: [
        {
          title: 'Задание 1: посчитать всё',
          difficulty: 'easy',
          lang: 'bash',
          description: 'В терминале выше посчитай, сколько строк во ВСЕХ трёх логах вместе взятых, одной командой. А потом — сколько строк с WARN во всех логах (тоже одним числом).',
          hints: [
            'cat умеет принимать несколько файлов и склеивать их — а шаблон *.log подставит все три.',
            'Счётчик строк — wc -l. Он же посчитает и то, что отобрал grep, если поставить его после ленты.',
          ],
          solution: `cat logs/*.log | wc -l          # 17
cat logs/*.log | grep WARN | wc -l   # 4   (или grep -rc WARN logs — по файлам)`,
        },
        {
          title: 'Задание 2: сводка по уровням',
          difficulty: 'medium',
          lang: 'bash',
          description: 'Построй сводку: сколько раз в логах встречается каждый уровень (INFO, WARN, ERROR), отсортированную так, чтобы самый частый уровень был первым. Уровень — второе слово в каждой строке.',
          hints: [
            'Четыре станка: склеить логи → вырезать вторую колонку → отсортировать (чтобы одинаковые встали рядом) → uniq -c.',
            'Чтобы самый частый оказался сверху, добавь пятый станок: ещё один sort, но по числам и в обратном порядке (-rn).',
          ],
          solution: `cat logs/*.log | cut -d " " -f 2 | sort | uniq -c | sort -rn
#      12 INFO
#       4 WARN
#       1 ERROR`,
        },
        {
          title: 'Задание 3: журнал предупреждений',
          difficulty: 'hard',
          lang: 'bash',
          description: 'Составь файл report.txt в домашнем каталоге: первой строкой — текст «Файлы с предупреждениями:», затем — только ИМЕНА файлов (без самих строк), в которых есть WARN, а в самом конце — все строки с WARN из всех логов с номерами строк. Файл errors.txt при этом не должен пострадать. Всё — только через > и >>.',
          hints: [
            'Первую строку создаёт echo с >. Всё остальное — дописывать через >>, иначе каждая следующая команда сотрёт предыдущее.',
            'У grep есть флаг, который печатает только имена файлов с совпадениями, а не сами строки; и другой флаг — для номеров строк.',
          ],
          solution: `echo "Файлы с предупреждениями:" > report.txt
grep -rl WARN logs >> report.txt
grep -rn WARN logs >> report.txt
cat report.txt`,
        },
      ],
      mistakes: [
        {
          wrong: 'Написать grep ERROR app.log > app.log, чтобы «оставить в логе только ошибки»',
          right: 'Файл будет пуст: > очищает его раньше, чем grep успевает прочитать. Результат всегда пишут в другой файл: grep ERROR app.log > errors.txt',
        },
        {
          wrong: 'Вызвать uniq -c без sort перед ним и удивляться, что одинаковые строки посчитаны по отдельности',
          right: 'uniq схлопывает только СОСЕДНИЕ повторы. Сначала sort ставит одинаковые строки рядом, потом uniq -c их считает: sort | uniq -c — всегда парой',
        },
      ],
      checklist: [
        'Объясняю конвейер | своими словами: вывод команды слева становится входом команды справа',
        'Умею grep (с -i, -n, -v, -c, -r), find по имени и типу, sort, uniq -c, wc -l, head, tail, cut',
        'Различаю find (ищет файлы по имени) и grep (ищет строки по содержимому) и умею соединять их через xargs',
        'Различаю > (перезаписать) и >> (дописать) и никогда не пишу результат в тот же файл, который читаю',
        'Могу одной строкой построить сводку «какая ошибка встречается чаще всего» из лога',
      ],
    },
  ],
};
