// Модуль 34 — Алгоритмы и структуры данных (часть 1: Big O, массивы/списки/стек/очередь,
// сортировки и поиск, хеш-таблицы). Весь код — чистый Python, bootstrap: null. Главный приём
// модуля: ученик не верит на слово, а замеряет time.time() сам и смотрит на ОТНОШЕНИЕ времён
// (абсолютные секунды зависят от его железа и браузера).

export const module34 = {
  id: 'dsa',
  order: 34,
  title: 'Алгоритмы и структуры данных',
  icon: '🧩',
  description:
    'Big O на пальцах и с секундомером, связные списки, стек и очередь, сортировки, бинарный поиск и хеш-таблицы — то, что спрашивают на алгоритмическом раунде собеседования.',
  lessons: [
    // ------------------------------------------------------------------
    // Урок 1. Big O
    // ------------------------------------------------------------------
    {
      id: 'big-o-intuition',
      title: 'Big O: почему «работает на 100 пользователях» ничего не значит',
      summary: 'Как оценивать, насколько быстро растёт время работы кода при росте данных, и почему это первый вопрос на алгоритмическом собеседовании',
      theory: [
        {
          type: 'p',
          text: 'Ты уже умеешь строить настоящие backend-системы. Но почти на любом техническом собеседовании есть отдельный раунд, где просят не спроектировать API, а решить небольшую задачу на алгоритмы — и спросить: «а какая у этого сложность?». Без ответа на этот вопрос даже правильное решение выглядит слабо. И это не только про собеседования: эндпоинт, который проверяет каждого пользователя против каждого, отлично работает на 100 пользователях в тестах и намертво вешает сервер на 100 000 в проде. Big O — язык, на котором об этом говорят.',
        },
        {
          type: 'analogy',
          text: 'Big O — это не «сколько шагов ты сделал», а «во сколько раз вырастет число шагов, если задача вырастет в 10 раз». Представь три способа найти человека в городе. Знаешь точный адрес — идёшь сразу к дому: город может вырасти хоть в сто раз, шагов столько же (O(1), константа). Ходишь по всем домам подряд, пока не найдёшь — город в 10 раз больше, шагов в 10 раз больше (O(n), линейно). Знакомишь каждого жителя с каждым — город в 10 раз больше, рукопожатий в 100 раз больше (O(n²), квадратично). Абсолютное время неважно: важно, как оно растёт.',
        },
        {
          type: 'steps',
          title: 'Пять классов, которых хватит на 95% вопросов',
          items: [
            { code: 'O(1) — my_list[5], my_dict["key"]', note: 'Константа: время не зависит от размера данных. Взять элемент по индексу, положить в словарь' },
            { code: 'O(log n) — бинарный поиск', note: 'Логарифм: каждый шаг отбрасывает половину. Миллион элементов — всего 20 шагов. Урок 3' },
            { code: 'O(n) — for x in my_list', note: 'Линейно: один проход по всем данным. Поиск в неотсортированном списке, sum(), max()' },
            { code: 'O(n log n) — sorted(my_list)', note: 'Чуть хуже линейного: так работают все хорошие сортировки. Урок 3' },
            { code: 'O(n²) — for i in ...: for j in ...', note: 'Квадрат: вложенный цикл по тем же данным. 1 000 элементов — миллион операций, 100 000 — десять миллиардов. Главный враг' },
          ],
        },
        {
          type: 'list',
          title: 'Как определить сложность, глядя на код',
          items: [
            'Нет циклов по данным, только арифметика и обращения по индексу/ключу — O(1).',
            'Один цикл по всем n элементам — O(n). Два цикла ПОДРЯД (не вложенных) — всё равно O(n): 2n — это «n, умноженное на константу», а константы Big O отбрасывает.',
            'Цикл внутри цикла по тем же данным — O(n²). Три вложенных — O(n³).',
            'На каждом шаге данные делятся пополам (бинарный поиск, деление отрезка) — O(log n).',
            'Внутри цикла по n элементам вызывается что-то за O(n) — например, x in my_list или my_list.index(x) — это скрытый вложенный цикл, O(n²). Самая частая ловушка.',
          ],
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'Константы не считаются — и это нормально',
          text: 'O(2n), O(n + 10), O(n/2) — всё это O(n). Big O описывает форму роста, а не точное число операций. Поэтому на собеседовании отвечай классом и объяснением: «O(n), потому что один проход по списку», а не пытайся посчитать операции до единицы. Если два решения оба O(n), но одно делает три прохода, а другое один — второе быстрее, но по Big O они равны.',
        },
        {
          type: 'callout',
          variant: 'warning',
          title: 'Про цифры в песочнице',
          text: 'Python в этом курсе выполняется прямо в браузере (Pyodide) — это медленнее, чем на твоём компьютере, и абсолютные секунды у всех разные. Поэтому смотри не на «0.3 секунды», а на ОТНОШЕНИЕ: если данных стало в 10 раз больше, а время выросло в 10 раз — это O(n); в 100 раз — O(n²); почти не выросло — O(1) или O(log n). Именно отношение и есть Big O, увиденное своими глазами.',
        },
      ],
      example: {
        title: 'Пять функций — пять классов сложности',
        lang: 'python',
        code: `def first_item(items):            # O(1): один шаг, размер списка не важен
    return items[0]

def find_index(items, target):    # O(n): в худшем случае проходим весь список
    for i, x in enumerate(items):
        if x == target:
            return i
    return -1

def has_duplicates(items):        # O(n²): каждый с каждым
    for i in range(len(items)):
        for j in range(i + 1, len(items)):
            if items[i] == items[j]:
                return True
    return False

def sort_items(items):            # O(n log n): встроенная сортировка (Timsort)
    return sorted(items)

def count_digits(n):              # O(log n): на каждом шаге число делится на 10
    steps = 0
    while n > 0:
        n //= 10
        steps += 1
    return steps`,
        explanation: 'На собеседовании тебя могут попросить оценить любую из них. Ответ всегда из двух частей: класс и почему. «has_duplicates — O(n²), потому что вложенный цикл по тем же данным».',
      },
      sandbox: {
        bootstrap: null,
        description:
          'Не верь на слово — замерь. Ниже линейный поиск запускается на списке из 100 и из 100 000 элементов. Данных в 1 000 раз больше — во сколько раз дольше? Поменяй размеры, повтори запуск: цифры чуть плавают, отношение — нет.',
        initialCode: `import time

def linear_search(items, target):
    """O(n): идём по списку, пока не найдём."""
    for i, x in enumerate(items):
        if x == target:
            return i
    return -1

def measure(size, repeats):
    """Среднее время ОДНОГО поиска. Маленький список ищем много раз подряд —
    иначе таймер (точность ~1 мс) покажет ноль."""
    items = list(range(size))
    target = size - 1              # худший случай: искомый — последний
    start = time.time()
    for _ in range(repeats):
        linear_search(items, target)
    return (time.time() - start) / repeats

small = measure(100, repeats=20_000)
big = measure(100_000, repeats=20)

print(f"100 элементов:     {small * 1000:.5f} мс на один поиск")
print(f"100 000 элементов: {big * 1000:.5f} мс на один поиск")
print(f"Данных в 1000 раз больше → время выросло примерно в {big / max(small, 1e-9):.0f} раз")
print("Рост того же порядка, что и рост данных (×1000) — линейный, O(n).")
print("(Перекос в 1.5–2 раза — фиксированная цена вызова функции, заметная только на крошечном списке.)")

# А теперь O(1): обращение по индексу не зависит от размера вообще
items_big = list(range(100_000))
start = time.time()
for _ in range(100_000):
    items_big[99_999]
print(f"\\n100 000 обращений по индексу: {(time.time() - start) * 1000:.1f} мс — размер списка не важен")`,
      },
      tasks: [
        {
          title: 'Задание 1: назови класс сложности',
          difficulty: 'easy',
          description: 'Для каждой функции определи Big O и объясни одной фразой почему: (а) сумма всех элементов списка через цикл; (б) проверка, есть ли элемент в множестве через x in my_set; (в) поиск пары элементов с одинаковым значением вложенным циклом; (г) печать первых 10 элементов списка любой длины.',
          hints: ['Считай циклы по данным: нет — O(1), один — O(n), вложенный — O(n²).', 'Первые 10 элементов — это 10 шагов независимо от того, 100 в списке или миллион.'],
          solution: `# (а) O(n) — один проход по всем элементам
# (б) O(1) — множество (set) ищет по хешу, без перебора (урок 4)
# (в) O(n²) — вложенный цикл по тем же данным
# (г) O(1) — всегда 10 шагов, размер списка не влияет`,
        },
        {
          title: 'Задание 2: докажи квадрат секундомером',
          difficulty: 'medium',
          description: 'Возьми функцию has_duplicates из примера (вложенный цикл). Замерь её на списке из 1 000 уникальных чисел, потом из 2 000. Данных в 2 раза больше — во сколько раз выросло время? Объясни, почему это подтверждает O(n²).',
          hints: ['list(range(n)) — n уникальных чисел; дубликатов нет, значит функция пройдёт все пары (худший случай).', 'При O(n²) удвоение n даёт рост времени в 2² = 4 раза. Если получилось 3.5–4.5 — всё верно.'],
          solution: `import time

def has_duplicates(items):
    for i in range(len(items)):
        for j in range(i + 1, len(items)):
            if items[i] == items[j]:
                return True
    return False

def measure(n):
    items = list(range(n))
    start = time.time()
    has_duplicates(items)
    return time.time() - start

t1 = measure(1000)
t2 = measure(2000)
print(f"1000: {t1:.3f}s, 2000: {t2:.3f}s, рост в {t2 / t1:.1f} раз")
print("Данных ×2 → время ×4: это квадрат, O(n²)")`,
        },
        {
          title: 'Задание 3: найди скрытый вложенный цикл',
          difficulty: 'hard',
          description: 'Функция ниже ищет общие элементы двух списков: for x in a: if x in b: result.append(x). Циклов на вид один — но какая у неё сложность на самом деле и почему? Перепиши так, чтобы стало O(n), и замерь обе версии на списках по 5 000 элементов (подсказка: одна из структур данных Python проверяет x in ... за O(1) — какая, узнаешь точно в уроке 4, но можешь попробовать угадать уже сейчас).',
          hints: ['x in b для списка — это скрытый цикл по b. Цикл в цикле — O(n²), хотя ключевое слово for написано один раз.', 'Преврати b в set один раз перед циклом: проверка x in set — O(1), итого O(n).'],
          solution: `import time

def common_slow(a, b):          # O(n²): "in" по списку — скрытый цикл
    result = []
    for x in a:
        if x in b:
            result.append(x)
    return result

def common_fast(a, b):          # O(n): "in" по множеству — O(1)
    b_set = set(b)
    return [x for x in a if x in b_set]

a = list(range(0, 5000))
b = list(range(2500, 7500))

start = time.time(); common_slow(a, b); slow = time.time() - start
start = time.time(); common_fast(a, b); fast = time.time() - start
print(f"список: {slow:.3f}s, множество: {fast:.4f}s — быстрее в {slow / max(fast, 1e-6):.0f} раз")`,
        },
      ],
      mistakes: [
        {
          wrong: 'Отвечать на собеседовании «это быстро, я проверял на своих данных»',
          right: 'Ответ всегда в терминах роста: «O(n), один проход по списку». Проверка на 100 элементах ничего не говорит о том, что будет на 100 000',
        },
        {
          wrong: 'Считать, что x in my_list — это «одна операция»',
          right: 'Для списка in — это перебор всех элементов, O(n). Внутри цикла это превращается в O(n²). Для set и dict — O(1), об этом урок 4',
        },
      ],
      checklist: [
        'Объясняю Big O как «во сколько раз вырастет время, если данных станет в 10 раз больше», а не как число операций',
        'Знаю пять классов O(1), O(log n), O(n), O(n log n), O(n²) и по примеру кода на каждый',
        'Определяю сложность по коду: нет циклов / один / вложенный / деление пополам',
        'Помню, что x in list — скрытый цикл O(n), и что константы Big O отбрасывает',
        'Умею подтвердить сложность замером: сравниваю отношение времён, а не абсолютные секунды',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 2. Массивы, связные списки, стек, очередь
    // ------------------------------------------------------------------
    {
      id: 'arrays-linked-lists-stack-queue',
      title: 'Массивы, связные списки, стек и очередь',
      summary: 'Как на самом деле устроен list в Python, зачем нужен связный список, и две структуры, о которых спрашивают всегда: стек (LIFO) и очередь (FIFO)',
      theory: [
        {
          type: 'p',
          text: 'Python-список кажется волшебным: добавляй что хочешь, доставай по индексу, вставляй в середину. Но под капотом это динамический массив — непрерывный кусок памяти с ячейками одинакового размера. Именно поэтому my_list[500000] находится мгновенно: адрес = начало + 500000 × размер ячейки, без перебора. А вот вставка в начало заставляет сдвинуть ВСЕ элементы на одну ячейку вправо — O(n). На собеседовании любят спросить именно про это.',
        },
        {
          type: 'analogy',
          text: 'Массив — это ряд пронумерованных шкафчиков в раздевалке, стоящих вплотную. Знаешь номер — идёшь прямо к нему (O(1)). Но чтобы втиснуть новый шкафчик в начало ряда, придётся подвинуть все остальные (O(n)). А если ряд закончился, а шкафчики ещё нужны — строят новый ряд побольше и перетаскивают всё туда: это и делает list.append(), когда место кончается. Связный список — другая идея: квест-записки. В каждой записке — значение и адрес, где лежит следующая. Вставить новую записку в начало — секунда (O(1)): написал одну записку и указал на старую первую. Зато чтобы найти пятую — придётся пройти четыре предыдущие (O(n)).',
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'append за O(1) — но «в среднем»',
          text: 'Когда в массиве кончается место, Python выделяет новый, примерно в 1.125 раза больше, и копирует туда всё — это O(n). Но происходит это редко (раз в много добавлений), поэтому В СРЕДНЕМ append стоит O(1) — говорят «амортизированная O(1)». Если на собеседовании скажешь «append — O(1), амортизированно, потому что переаллокация редкая» — это плюс в карму. insert(0, x) и pop(0) — честные O(n): сдвиг всех элементов.',
        },
        {
          type: 'steps',
          title: 'Связный список руками — классика, которую пишут хотя бы раз',
          items: [
            { code: 'class Node: value, next', note: 'Узел-«записка»: значение и ссылка на следующий узел (None — если он последний)' },
            { code: 'class LinkedList: head = None', note: 'Сам список хранит только голову — первую записку. Всё остальное достижимо по ссылкам' },
            { code: 'push_front(v): node.next = self.head; self.head = node', note: 'Вставка в начало за O(1): новая записка указывает на старую первую' },
            { code: 'while node: ...; node = node.next', note: 'Обход: идём по ссылкам, пока не упрёмся в None. Так устроены и поиск, и печать, и подсчёт длины — все O(n)' },
            { code: 'reverse(): prev, cur = None, head; ...', note: 'Разворот: перевешиваем каждую стрелку в обратную сторону. Задание 3 этого урока и вопрос №1 про связные списки на интервью' },
          ],
        },
        {
          type: 'analogy',
          text: 'Стек — стопка тарелок: кладёшь сверху, берёшь сверху; последняя положенная уходит первой (LIFO — last in, first out). Так работает кнопка «Назад» в браузере, Ctrl+Z и вызовы функций в Python (стек вызовов — отсюда «traceback»). Очередь — очередь в кассу: кто первый пришёл, тот первый обслужен (FIFO — first in, first out). Так работают задачи в Celery из модуля 23 и запросы, ждущие сервер.',
        },
        {
          type: 'list',
          title: 'Чем реализовать в Python',
          items: [
            'Стек — обычный list: append() положить наверх, pop() снять сверху, [-1] посмотреть верх. Обе операции O(1), ничего лучше не нужно.',
            'Очередь — collections.deque: append() встать в конец, popleft() выйти из начала — оба O(1). deque — двусторонняя очередь, устроена так, что оба конца быстрые.',
            'НЕ делай очередь на list с pop(0): каждый pop(0) сдвигает весь список — O(n), на большой очереди это в сотни раз медленнее deque.',
            'Связный список в проде почти не пишут руками (deque внутри — почти он и есть), но на собеседовании его просят реализовать, чтобы проверить, понимаешь ли ты ссылки и указатели.',
          ],
        },
      ],
      example: {
        title: 'Связный список с обходом и разворотом',
        lang: 'python',
        code: `class Node:
    def __init__(self, value, next=None):
        self.value = value
        self.next = next            # ссылка на следующий узел или None


class LinkedList:
    def __init__(self):
        self.head = None

    def push_front(self, value):    # O(1)
        self.head = Node(value, self.head)

    def __iter__(self):             # обход: for x in linked_list
        node = self.head
        while node:
            yield node.value
            node = node.next

    def reverse(self):              # O(n), без дополнительной памяти
        prev, cur = None, self.head
        while cur:
            nxt = cur.next          # запомнить следующего, пока не перевесили стрелку
            cur.next = prev         # развернуть стрелку назад
            prev, cur = cur, nxt    # шагнуть вперёд
        self.head = prev


ll = LinkedList()
for v in [3, 2, 1]:
    ll.push_front(v)
print(list(ll))       # [1, 2, 3]
ll.reverse()
print(list(ll))       # [3, 2, 1]`,
        explanation: 'В reverse три строки внутри цикла — и порядок именно такой: сначала запомнить следующего, потом перевесить стрелку, потом шагнуть. Поменяй порядок — потеряешь хвост списка. Это ровно то место, где на собеседовании просят «нарисуй на бумаге, что происходит с указателями».',
      },
      sandbox: {
        bootstrap: null,
        description:
          'Стек, очередь и разница между list.pop(0) и deque.popleft() — с секундомером. Запусти и посмотри, во сколько раз «очередь на списке» медленнее настоящей.',
        initialCode: `import time
from collections import deque

# --- Стек: стопка тарелок (LIFO) ---
stack = []
stack.append("страница 1")
stack.append("страница 2")
stack.append("страница 3")
print("Верх стека:", stack[-1])
print("Кнопка «Назад»:", stack.pop())       # снимаем верхнюю
print("Осталось:", stack)

# --- Очередь: очередь в кассу (FIFO) ---
queue = deque()
queue.append("Аня")
queue.append("Борис")
queue.append("Вика")
print("\\nПервый в очереди:", queue.popleft())
print("Осталось:", list(queue))

# --- Секундомер: очередь на list vs deque ---
N = 30_000

bad_queue = list(range(N))
start = time.time()
while bad_queue:
    bad_queue.pop(0)              # O(n): сдвигает все элементы каждый раз
slow = time.time() - start

good_queue = deque(range(N))
start = time.time()
while good_queue:
    good_queue.popleft()          # O(1)
fast = time.time() - start

print(f"\\n{N} извлечений из очереди:")
print(f"  list.pop(0):     {slow * 1000:.1f} мс")
print(f"  deque.popleft(): {fast * 1000:.1f} мс")
print(f"  deque быстрее в {slow / max(fast, 1e-9):.0f} раз — и разрыв растёт с размером очереди")`,
      },
      tasks: [
        {
          title: 'Задание 1: найти и посчитать в связном списке',
          difficulty: 'easy',
          description: 'Добавь в класс LinkedList из примера два метода: __len__ (сколько узлов) и find(value) — возвращает True, если значение есть в списке. Оба — обходом по ссылкам. Какая у них сложность и почему она хуже, чем у len(list) и list[i]?',
          hints: ['Шаблон обхода: node = self.head; while node: ...; node = node.next.', 'len(list) — O(1), потому что Python хранит длину массива отдельно. У связного списка длину можно узнать, только пройдя его весь — O(n). Так же и с доступом по номеру.'],
          solution: `class Node:
    def __init__(self, value, next=None):
        self.value = value
        self.next = next

class LinkedList:
    def __init__(self):
        self.head = None

    def push_front(self, value):
        self.head = Node(value, self.head)

    def __len__(self):                # O(n)
        count, node = 0, self.head
        while node:
            count += 1
            node = node.next
        return count

    def find(self, value):            # O(n)
        node = self.head
        while node:
            if node.value == value:
                return True
            node = node.next
        return False

ll = LinkedList()
for v in [10, 20, 30]:
    ll.push_front(v)
print(len(ll), ll.find(20), ll.find(99))   # 3 True False`,
        },
        {
          title: 'Задание 2: сбалансированные скобки (классика интервью)',
          difficulty: 'medium',
          description: 'Напиши функцию is_balanced(s), которая проверяет, правильно ли расставлены скобки трёх видов: "({[]})" → True, "([)]" → False, "((" → False, "" → True. Используй стек: открывающую — кладём, закрывающую — сравниваем с верхом стека.',
          hints: ['Словарь пар: pairs = {")": "(", "]": "[", "}": "{"}. Встретил закрывающую — верх стека должен быть её парой, иначе False.', 'В конце стек должен быть пуст: незакрытые скобки — тоже False. И не забудь случай, когда закрывающая пришла, а стек уже пуст.'],
          solution: `def is_balanced(s):
    pairs = {")": "(", "]": "[", "}": "{"}
    stack = []
    for ch in s:
        if ch in "([{":
            stack.append(ch)
        elif ch in pairs:
            if not stack or stack[-1] != pairs[ch]:
                return False
            stack.pop()
    return not stack

for test, expected in [("({[]})", True), ("([)]", False), ("((", False), ("", True), ("]", False)]:
    result = is_balanced(test)
    print(f"{test!r:10} → {result}  {'✓' if result == expected else '✗'}")`,
        },
        {
          title: 'Задание 3: развернуть связный список',
          difficulty: 'hard',
          description: 'Не подглядывая в пример, напиши метод reverse() для LinkedList из задания 1 — итеративно, за O(n) и без создания нового списка. Проверь: [1, 2, 3] → [3, 2, 1]. Потом ответь: что случится, если в цикле сначала сделать cur.next = prev, а только потом запомнить nxt = cur.next?',
          hints: ['Три переменные: prev (уже развёрнутая часть), cur (текущий), nxt (ещё не тронутый хвост). На каждом шаге стрелка cur.next перевешивается на prev.', 'Если перевесить стрелку до того, как запомнил nxt — ссылка на хвост потеряна: cur.next теперь смотрит назад, и хвост списка исчез навсегда.'],
          solution: `class Node:
    def __init__(self, value, next=None):
        self.value = value
        self.next = next

class LinkedList:
    def __init__(self):
        self.head = None

    def push_front(self, value):
        self.head = Node(value, self.head)

    def __iter__(self):
        node = self.head
        while node:
            yield node.value
            node = node.next

    def reverse(self):
        prev, cur = None, self.head
        while cur:
            nxt = cur.next       # 1. запомнить хвост
            cur.next = prev      # 2. перевесить стрелку
            prev, cur = cur, nxt # 3. шагнуть
        self.head = prev

ll = LinkedList()
for v in [3, 2, 1]:
    ll.push_front(v)
print(list(ll))    # [1, 2, 3]
ll.reverse()
print(list(ll))    # [3, 2, 1]
# Если перевесить стрелку раньше, чем запомнить nxt, — cur.next уже указывает
# назад, хвост списка потерян, цикл остановится после первого узла.`,
        },
      ],
      mistakes: [
        {
          wrong: 'Делать очередь на обычном list и забирать элементы через pop(0)',
          right: 'pop(0) сдвигает весь список — O(n) на каждое извлечение. Для очереди — collections.deque с popleft() за O(1)',
        },
        {
          wrong: 'Говорить «append всегда O(1)» без оговорки',
          right: 'append — амортизированная O(1): изредка массив переаллоцируется и копируется целиком (O(n)), но это происходит так редко, что в среднем на одно добавление приходится константа',
        },
      ],
      checklist: [
        'Понимаю, что list — динамический массив: индекс O(1), вставка/удаление в начале O(n), append амортизированная O(1)',
        'Могу написать Node и LinkedList с нуля: push_front, обход, длина, поиск, разворот',
        'Знаю, что у связного списка вставка в начало O(1), а доступ по номеру O(n) — ровно наоборот массиву',
        'Стек = LIFO на list (append/pop), очередь = FIFO на deque (append/popleft); никогда pop(0) для очереди',
        'Решаю задачу о сбалансированных скобках через стек',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 3. Сортировки и поиск
    // ------------------------------------------------------------------
    {
      id: 'sorting-and-binary-search',
      title: 'Сортировки и бинарный поиск',
      summary: 'Пузырёк, чтобы понять механику; сортировка слиянием, чтобы понять O(n log n); бинарный поиск — 20 шагов на миллион элементов и ошибка, которую делают все',
      theory: [
        {
          type: 'p',
          text: 'В проде ты никогда не напишешь сортировку сам — sorted() в Python (алгоритм Timsort) быстрее и надёжнее всего, что ты сделаешь руками. Зачем тогда учить? Потому что на собеседовании через сортировку проверяют, понимаешь ли ты, откуда берётся O(n log n) и чем оно лучше O(n²), — а бинарный поиск ты будешь использовать напрямую: в отсортированных данных он находит элемент за 20 шагов там, где перебор делает миллион.',
        },
        {
          type: 'analogy',
          text: 'Сортировка пузырьком — ты идёшь вдоль книжной полки и меняешь местами каждую пару соседей, стоящих не по порядку; и так проход за проходом, пока не останется ни одной неправильной пары. Работает, но полка в 10 раз длиннее — работы в 100 раз больше (O(n²)). Сортировка слиянием — разделяй и властвуй: разрезаешь колоду карт пополам, каждую половину — ещё пополам, пока не останутся стопки по одной карте (они уже «отсортированы»). А потом сливаешь стопки попарно: смотришь на две верхние карты, берёшь меньшую. Половинок — log n уровней, на каждом уровне n карт — O(n log n).',
        },
        {
          type: 'steps',
          title: 'Бинарный поиск: угадай число от 1 до 1 000 000 за 20 попыток',
          items: [
            { code: 'low, high = 0, len(items) - 1', note: 'Границы отрезка, где может быть ответ. Работает ТОЛЬКО на отсортированном списке' },
            { code: 'while low <= high:', note: 'Пока отрезок не пуст. Именно <=, а не < — иначе отрезок из одного элемента не проверится (см. «Типичные ошибки»)' },
            { code: 'mid = (low + high) // 2', note: 'Середина. В Python переполнения нет; в Java/C++ пишут low + (high - low) // 2 — могут спросить, почему' },
            { code: 'if items[mid] == target: return mid', note: 'Попали' },
            { code: 'elif items[mid] < target: low = mid + 1', note: 'Искомое правее — отбрасываем левую половину вместе с mid' },
            { code: 'else: high = mid - 1', note: 'Искомое левее — отбрасываем правую половину вместе с mid' },
            { code: 'return -1', note: 'Отрезок опустел — элемента нет' },
          ],
        },
        {
          type: 'list',
          title: 'Сложности, которые нужно знать наизусть',
          items: [
            'Пузырёк, вставками, выбором — O(n²). Учебные; в проде не используют.',
            'Слиянием (merge sort), быстрая (quicksort), sorted()/list.sort() в Python (Timsort) — O(n log n). Timsort ещё и умеет пользоваться уже отсортированными кусками — на «почти отсортированных» данных близок к O(n).',
            'Линейный поиск — O(n), не требует сортировки. Бинарный — O(log n), но требует отсортированного списка: сортировать ради одного поиска (O(n log n)) бессмысленно, ради тысячи поисков — обязательно.',
            'sorted(items, key=...) — сортировать словари/объекты по полю: sorted(users, key=lambda u: u["age"]). reverse=True — по убыванию.',
          ],
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'bisect — бинарный поиск «как в проде»',
          text: 'В стандартной библиотеке есть модуль bisect: bisect.bisect_left(items, x) возвращает позицию, куда вставить x, чтобы список остался отсортированным (если x есть — позицию первого такого), а bisect.insort(items, x) сразу вставляет. Так делают, например, поиск нужного тарифа по порогам цены или ближайшей временной метки в логе. Руками бинарный поиск пишут на собеседовании, в коде — bisect.',
        },
      ],
      example: {
        title: 'Три сортировки и два поиска',
        lang: 'python',
        code: `import bisect

def bubble_sort(items):                 # O(n²)
    a = items[:]                        # копия, чтобы не портить исходный
    n = len(a)
    for i in range(n):
        for j in range(n - 1 - i):      # после i проходов хвост уже на месте
            if a[j] > a[j + 1]:
                a[j], a[j + 1] = a[j + 1], a[j]
    return a

def merge_sort(items):                  # O(n log n)
    if len(items) <= 1:
        return items
    mid = len(items) // 2
    left = merge_sort(items[:mid])      # разделяй...
    right = merge_sort(items[mid:])
    merged, i, j = [], 0, 0
    while i < len(left) and j < len(right):   # ...и сливай: берём меньшую из двух верхних
        if left[i] <= right[j]:
            merged.append(left[i]); i += 1
        else:
            merged.append(right[j]); j += 1
    merged.extend(left[i:])
    merged.extend(right[j:])
    return merged

def binary_search(items, target):       # O(log n), items отсортирован
    low, high = 0, len(items) - 1
    while low <= high:
        mid = (low + high) // 2
        if items[mid] == target:
            return mid
        elif items[mid] < target:
            low = mid + 1
        else:
            high = mid - 1
    return -1

data = [5, 2, 9, 1, 7]
print(bubble_sort(data), merge_sort(data), sorted(data))   # одно и то же
print(binary_search([1, 2, 5, 7, 9], 7))                   # 3
print(bisect.bisect_left([1, 2, 5, 7, 9], 6))              # 3 — куда вставить 6`,
        explanation: 'Обрати внимание на условие в merge: left[i] <= right[j], а не <. Это делает сортировку устойчивой — равные элементы сохраняют исходный порядок. На собеседовании могут спросить, что такое устойчивость сортировки: вот ответ.',
      },
      sandbox: {
        bootstrap: null,
        description:
          'Один и тот же список из 2 000 случайных чисел — три сортировки с секундомером. Потом бинарный поиск против линейного на миллионе элементов. Смотри на отношения: они одинаковы на любом компьютере.',
        initialCode: `import random
import time

def bubble_sort(items):
    a = items[:]
    n = len(a)
    for i in range(n):
        for j in range(n - 1 - i):
            if a[j] > a[j + 1]:
                a[j], a[j + 1] = a[j + 1], a[j]
    return a

def merge_sort(items):
    if len(items) <= 1:
        return items
    mid = len(items) // 2
    left, right = merge_sort(items[:mid]), merge_sort(items[mid:])
    merged, i, j = [], 0, 0
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            merged.append(left[i]); i += 1
        else:
            merged.append(right[j]); j += 1
    merged.extend(left[i:]); merged.extend(right[j:])
    return merged

def binary_search(items, target):
    low, high = 0, len(items) - 1
    while low <= high:
        mid = (low + high) // 2
        if items[mid] == target:
            return mid
        elif items[mid] < target:
            low = mid + 1
        else:
            high = mid - 1
    return -1

def timed(fn, *args):
    start = time.time()
    result = fn(*args)
    return result, time.time() - start

data = [random.randint(0, 1_000_000) for _ in range(2000)]

bubble, t_bubble = timed(bubble_sort, data)
merged, t_merge = timed(merge_sort, data)
built_in, t_sorted = timed(sorted, data)
assert bubble == merged == built_in, "все три должны дать одинаковый результат"

print("Сортировка 2 000 элементов:")
print(f"  пузырёк  O(n²):      {t_bubble * 1000:8.1f} мс")
print(f"  слиянием O(n log n): {t_merge * 1000:8.1f} мс   (в {t_bubble / max(t_merge, 1e-9):.0f} раз быстрее пузырька)")
print(f"  sorted() O(n log n): {t_sorted * 1000:8.2f} мс   (Timsort на C — вне конкуренции)")

# --- Поиск в миллионе элементов ---
million = list(range(1_000_000))
target = 999_999                      # худший случай для линейного
REPEATS = 20

start = time.time()
for _ in range(REPEATS):
    million.index(target)             # линейный, O(n)
t_linear = (time.time() - start) / REPEATS

start = time.time()
for _ in range(REPEATS):
    binary_search(million, target)    # O(log n)
t_binary = (time.time() - start) / REPEATS

print(f"\\nПоиск в 1 000 000 элементов:")
print(f"  линейный:  {t_linear * 1000:.3f} мс")
print(f"  бинарный:  {t_binary * 1000:.4f} мс — около 20 шагов вместо миллиона")`,
      },
      tasks: [
        {
          title: 'Задание 1: bisect вместо ручного поиска',
          difficulty: 'easy',
          description: 'Есть отсортированный список порогов скидок thresholds = [1000, 5000, 10000, 50000] (сумма заказа) и скидки discounts = [0, 5, 10, 15, 20] в процентах (на одну больше: для сумм ниже первого порога и выше последнего). Через bisect.bisect_right напиши функцию discount_for(amount), которая возвращает процент скидки. Проверь на 500, 1000, 7000, 99999.',
          hints: ['bisect_right(thresholds, amount) возвращает, сколько порогов не больше amount — это и есть индекс в discounts.', 'Для 1000 ответ 5 (порог 1000 достигнут), для 999 — 0.'],
          solution: `import bisect

thresholds = [1000, 5000, 10000, 50000]
discounts = [0, 5, 10, 15, 20]

def discount_for(amount):
    return discounts[bisect.bisect_right(thresholds, amount)]

for amount in [500, 1000, 7000, 99999]:
    print(amount, "→", discount_for(amount), "%")   # 0 5 10 20`,
        },
        {
          title: 'Задание 2: сортировка вставками и её честный замер',
          difficulty: 'medium',
          description: 'Реализуй сортировку вставками (insertion sort): идёшь слева направо, каждый новый элемент «вдвигаешь» на своё место в уже отсортированную левую часть, сдвигая большие элементы вправо. Замерь на 2 000 случайных чисел и на 2 000 УЖЕ отсортированных. Почему второй замер во много раз быстрее, хотя алгоритм тот же O(n²)?',
          hints: ['for i in range(1, n): key = a[i]; j = i - 1; while j >= 0 and a[j] > key: a[j + 1] = a[j]; j -= 1; a[j + 1] = key.', 'На отсортированных данных внутренний while ни разу не выполняется — получается O(n). Big O описывает худший случай; лучший может быть куда лучше. Timsort использует именно это свойство.'],
          solution: `import random, time

def insertion_sort(items):
    a = items[:]
    for i in range(1, len(a)):
        key, j = a[i], i - 1
        while j >= 0 and a[j] > key:
            a[j + 1] = a[j]
            j -= 1
        a[j + 1] = key
    return a

data = [random.randint(0, 10**6) for _ in range(2000)]
start = time.time(); insertion_sort(data); t_random = time.time() - start
start = time.time(); insertion_sort(sorted(data)); t_sorted = time.time() - start
print(f"случайные: {t_random * 1000:.1f} мс, отсортированные: {t_sorted * 1000:.1f} мс")
print("на отсортированных данных внутренний цикл не работает — лучший случай O(n)")`,
        },
        {
          title: 'Задание 3: первое вхождение (бинарный поиск с подвохом)',
          difficulty: 'hard',
          description: 'В отсортированном списке с повторами [1, 2, 2, 2, 3, 5, 5] обычный бинарный поиск для 2 может вернуть индекс 1, 2 или 3 — какой попадётся. Напиши first_occurrence(items, target), которая возвращает индекс ПЕРВОГО вхождения (тут — 1) за O(log n), или -1. Подсказка: найдя совпадение, не останавливайся — запомни его и продолжай искать левее. Сверь результат с bisect.bisect_left.',
          hints: ['При items[mid] == target: result = mid; high = mid - 1 — и цикл продолжается в левой половине.', 'bisect_left возвращает ту же позицию, если элемент есть; проверь, что items[pos] == target.'],
          solution: `import bisect

def first_occurrence(items, target):
    low, high, result = 0, len(items) - 1, -1
    while low <= high:
        mid = (low + high) // 2
        if items[mid] == target:
            result = mid            # запомнили и идём левее
            high = mid - 1
        elif items[mid] < target:
            low = mid + 1
        else:
            high = mid - 1
    return result

items = [1, 2, 2, 2, 3, 5, 5]
for t in [2, 5, 1, 4]:
    pos = bisect.bisect_left(items, t)
    via_bisect = pos if pos < len(items) and items[pos] == t else -1
    print(t, "→", first_occurrence(items, t), "bisect:", via_bisect)   # 1 5 0 -1`,
        },
      ],
      mistakes: [
        {
          wrong: 'Писать в бинарном поиске while low < high вместо while low <= high',
          right: 'Когда отрезок сузился до одного элемента, low == high — и с условием < цикл завершится, не проверив его. Элемент, стоящий последним в поиске, «не найдётся». С <= последний элемент проверяется, а выход происходит, когда low перепрыгнул high — отрезок действительно пуст',
        },
        {
          wrong: 'Сортировать список внутри цикла или при каждом запросе к API — «чтобы бинарный поиск работал»',
          right: 'Сортировка — O(n log n), дороже одного линейного поиска. Отсортируй один раз при загрузке данных (или храни отсортированным через bisect.insort), а потом ищи много раз за O(log n)',
        },
      ],
      checklist: [
        'Могу написать пузырёк и сортировку слиянием и объяснить, откуда O(n²) и O(n log n)',
        'Пишу бинарный поиск без ошибок: low <= high, mid + 1 / mid - 1, только на отсортированных данных',
        'Знаю, что sorted() — Timsort, O(n log n), и что руками сортировки в проде не пишут',
        'Использую bisect для поиска позиции/порога и понимаю, что сортировать ради одного поиска не имеет смысла',
        'Могу объяснить устойчивость сортировки и лучший/худший случай (вставками на отсортированных данных — O(n))',
      ],
    },

    // ------------------------------------------------------------------
    // Урок 4. Хеш-таблицы
    // ------------------------------------------------------------------
    {
      id: 'hash-tables-dict-set',
      title: 'Хеш-таблицы: почему dict и set спасают от O(n²)',
      summary: 'Как словарь находит ключ за O(1), классическая задача Two Sum двумя способами, и коллизии на уровне интуиции',
      theory: [
        {
          type: 'p',
          text: 'Самая полезная структура данных для backend-разработчика — и самая частая на собеседованиях. Половина «оптимизируй это решение» сводится к одному приёму: заменить перебор списка на поиск в словаре или множестве. Ты пользуешься dict с модуля 2, но теперь важно понять, почему my_dict["key"] — это O(1), а не O(n), и как этим пользоваться сознательно.',
        },
        {
          type: 'analogy',
          text: 'Гардероб в театре. Плохой гардеробщик ищет твоё пальто, перебирая вешалки подряд, — чем больше гостей, тем дольше (O(n), поиск в списке). Хороший гардеробщик даёт номерок: номер вычисляется из самого пальто (хеш-функция), и вешалка с этим номером — единственное место, где оно может висеть. Пришёл с номерком — сразу к нужной вешалке, хоть в гардеробе сто пальто, хоть миллион (O(1)). Так устроены dict и set: hash(ключ) → номер ячейки → сразу туда.',
        },
        {
          type: 'steps',
          title: 'Что происходит при my_dict["alice"]',
          items: [
            { code: 'hash("alice")  →  большое число', note: 'Хеш-функция превращает ключ в число. Для одного и того же ключа число всегда одно и то же' },
            { code: 'число % размер_таблицы  →  номер ячейки', note: 'Из числа получаем индекс во внутреннем массиве — как индекс в list, O(1)' },
            { code: 'сравнить ключ в ячейке с "alice"', note: 'Проверка, что это действительно наш ключ, а не сосед с таким же номером' },
            { code: 'вернуть значение', note: 'Итого: пара арифметических операций и одно сравнение — независимо от размера словаря' },
          ],
        },
        {
          type: 'callout',
          variant: 'info',
          title: 'Коллизии: два пальто на один номерок',
          text: 'Хешей бесконечно много, а ячеек — конечное число, поэтому иногда два разных ключа попадают в одну ячейку — это коллизия. Python в таком случае ищет следующую свободную ячейку по особому правилу, и при поиске проверяет несколько ячеек подряд. Пока коллизий мало, это по-прежнему O(1) «в среднем». Чтобы их было мало, dict сам расширяет таблицу, когда она заполняется на две трети (так же, как list растёт при append). Худший случай — O(n), если все ключи попали в одну ячейку, — на практике не встречается. На собеседовании достаточно сказать: «O(1) в среднем, O(n) в худшем, из-за коллизий».',
        },
        {
          type: 'list',
          title: 'Что O(1), а что нет',
          items: [
            'dict: d[k], d[k] = v, k in d, del d[k] — O(1) в среднем. set: add, remove, x in s — тоже O(1).',
            'list: x in my_list, my_list.index(x), my_list.remove(x) — O(n). Если проверок много — сначала set(my_list), потом проверяй.',
            'Ключами dict и элементами set могут быть только неизменяемые (хешируемые) объекты: str, int, tuple, frozenset. list и dict — нельзя: TypeError: unhashable type. Нужен ключ из списка — превращай в tuple.',
            'Обход всего словаря (for k in d) — по-прежнему O(n): хеш ускоряет поиск по ключу, а не перебор.',
            'Цена — память: dict/set хранят хеши и держат запас пустых ячеек, поэтому весят больше list. Обычно это выгодный обмен.',
          ],
        },
      ],
      example: {
        title: 'Two Sum — эталонная задача: наивно и через словарь',
        lang: 'python',
        code: `# Задача: даны список чисел и target. Вернуть индексы двух чисел, дающих в сумме target.
# nums = [2, 7, 11, 15], target = 9  →  (0, 1), потому что 2 + 7 = 9

def two_sum_slow(nums, target):          # O(n²): каждая пара
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            if nums[i] + nums[j] == target:
                return (i, j)
    return None

def two_sum_fast(nums, target):          # O(n): один проход + словарь
    seen = {}                            # число → его индекс
    for i, x in enumerate(nums):
        need = target - x                # какое число нам нужно в пару
        if need in seen:                 # O(1): уже встречали?
            return (seen[need], i)
        seen[x] = i                      # запомнить текущее
    return None

print(two_sum_slow([2, 7, 11, 15], 9))   # (0, 1)
print(two_sum_fast([2, 7, 11, 15], 9))   # (0, 1)`,
        explanation: 'Приём универсален: вместо «для каждого элемента искать пару перебором» — «идти по списку один раз и запоминать увиденное в словаре, чтобы пару находить за O(1)». Именно так объясняй на собеседовании: «меняю время на память — словарь стоит O(n) памяти, зато время падает с O(n²) до O(n)».',
      },
      sandbox: {
        bootstrap: null,
        description:
          'Поиск дубликатов: вложенный цикл против set — на одном и том же списке. Потом set на 50 000 элементов — чтобы увидеть, что ему всё равно. И Two Sum в двух версиях с секундомером.',
        initialCode: `import random
import time

def duplicates_slow(items):          # O(n²)
    found = set()
    for i in range(len(items)):
        for j in range(i + 1, len(items)):
            if items[i] == items[j]:
                found.add(items[i])
    return found

def duplicates_fast(items):          # O(n)
    seen, found = set(), set()
    for x in items:
        if x in seen:                # O(1)
            found.add(x)
        seen.add(x)
    return found

def timed(fn, *args):
    start = time.time()
    result = fn(*args)
    return result, time.time() - start

data = [random.randint(0, 5000) for _ in range(4000)]

slow_result, t_slow = timed(duplicates_slow, data)
fast_result, t_fast = timed(duplicates_fast, data)
assert slow_result == fast_result

print(f"Дубликаты в списке из {len(data)} элементов (нашли {len(fast_result)}):")
print(f"  вложенный цикл O(n²): {t_slow * 1000:8.1f} мс")
print(f"  через set O(n):       {t_fast * 1000:8.2f} мс   → быстрее в {t_slow / max(t_fast, 1e-9):.0f} раз")

big = [random.randint(0, 100_000) for _ in range(50_000)]
_, t_big = timed(duplicates_fast, big)
print(f"\\nset на 50 000 элементов: {t_big * 1000:.1f} мс")
print(f"вложенному циклу на 50 000 понадобилось бы примерно в {(50_000 / len(data)) ** 2:.0f} раз дольше, чем на {len(data)} — минуты")

# --- Two Sum ---
def two_sum_slow(nums, target):
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            if nums[i] + nums[j] == target:
                return (i, j)
    return None

def two_sum_fast(nums, target):
    seen = {}
    for i, x in enumerate(nums):
        if target - x in seen:
            return (seen[target - x], i)
        seen[x] = i
    return None

nums = random.sample(range(1, 10_000_000), 3000)
target = nums[-1] + nums[-2]           # ответ — последняя пара: худший случай для перебора

pair_slow, t1 = timed(two_sum_slow, nums, target)
pair_fast, t2 = timed(two_sum_fast, nums, target)
print(f"\\nTwo Sum на {len(nums)} числах: перебор {t1 * 1000:.1f} мс, словарь {t2 * 1000:.2f} мс → в {t1 / max(t2, 1e-9):.0f} раз быстрее")
print("ответы:", pair_slow, pair_fast)`,
      },
      tasks: [
        {
          title: 'Задание 1: частота слов',
          difficulty: 'easy',
          description: 'Дана строка text = "кот пёс кот рыба пёс кот". Посчитай, сколько раз встречается каждое слово, через dict за O(n), и выведи слова по убыванию частоты. Потом сделай то же самое одной строкой через collections.Counter и его метод most_common().',
          hints: ['counts[word] = counts.get(word, 0) + 1 — классический приём; или defaultdict(int).', 'sorted(counts.items(), key=lambda kv: kv[1], reverse=True).'],
          solution: `from collections import Counter

text = "кот пёс кот рыба пёс кот"
counts = {}
for word in text.split():
    counts[word] = counts.get(word, 0) + 1
print(sorted(counts.items(), key=lambda kv: kv[1], reverse=True))

print(Counter(text.split()).most_common())   # [('кот', 3), ('пёс', 2), ('рыба', 1)]`,
        },
        {
          title: 'Задание 2: первый неповторяющийся символ',
          difficulty: 'medium',
          description: 'Напиши first_unique(s), которая возвращает первый символ строки, встречающийся ровно один раз, или None: "abcab" → "c", "aabb" → None. Решение должно быть O(n): два прохода — посчитать, потом найти. Объясни, почему решение через s.count(ch) внутри цикла — O(n²).',
          hints: ['Первый проход — Counter или dict частот. Второй — for ch in s: if counts[ch] == 1: return ch.', 's.count(ch) сам проходит всю строку — скрытый цикл в цикле.'],
          solution: `from collections import Counter

def first_unique(s):
    counts = Counter(s)              # O(n)
    for ch in s:                     # O(n)
        if counts[ch] == 1:
            return ch
    return None

print(first_unique("abcab"))         # c
print(first_unique("aabb"))          # None
# s.count(ch) в цикле — O(n) внутри O(n) = O(n²)`,
        },
        {
          title: 'Задание 3: группировка анаграмм',
          difficulty: 'hard',
          description: 'Дан список слов ["eat", "tea", "tan", "ate", "nat", "bat"]. Сгруппируй анаграммы (слова из одних и тех же букв): [["eat","tea","ate"], ["tan","nat"], ["bat"]]. Ключ к решению — придумать для каждого слова такой ключ словаря, чтобы у анаграмм он совпадал. Почему ключом не может быть list?',
          hints: ['"".join(sorted(word)) — у "eat", "tea", "ate" это "aet". Все анаграммы — под одним ключом.', 'list нехешируемый (TypeError: unhashable type). Ключ должен быть str или tuple.'],
          solution: `from collections import defaultdict

def group_anagrams(words):
    groups = defaultdict(list)
    for word in words:
        key = "".join(sorted(word))      # "eat" → "aet"; str — хешируемый ключ
        groups[key].append(word)
    return list(groups.values())

print(group_anagrams(["eat", "tea", "tan", "ate", "nat", "bat"]))
# [['eat', 'tea', 'ate'], ['tan', 'nat'], ['bat']]
# list в качестве ключа нельзя: TypeError: unhashable type: 'list'`,
        },
      ],
      mistakes: [
        {
          wrong: 'Проверять if user_id in users_list внутри цикла по запросам — «список же маленький»',
          right: 'Список растёт, и O(n²) настигает в проде. Один раз users_set = set(users_list) — и каждая проверка O(1)',
        },
        {
          wrong: 'Использовать list или dict в качестве ключа словаря и получать TypeError: unhashable type',
          right: 'Ключ должен быть неизменяемым: превращай список в tuple, а «составной ключ» делай кортежем (user_id, date)',
        },
      ],
      checklist: [
        'Объясняю, почему dict/set ищут за O(1): хеш → номер ячейки → сравнение',
        'Знаю, что такое коллизия, и отвечаю «O(1) в среднем, O(n) в худшем»',
        'Решаю Two Sum за O(n) через словарь и умею объяснить обмен памяти на время',
        'Заменяю x in list на x in set, когда проверок много; помню про нехешируемые list/dict',
        'Использую dict/Counter/defaultdict для подсчёта частот и группировки за O(n)',
      ],
    },
  ],
};
